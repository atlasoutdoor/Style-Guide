{
  "name": "@atlasoutdoor/Style-Guide",
  "version": "1.0",
  "description": "A style guide for Atlas Companies (Atlas Outdoor, Atlas Outdoor Living, and Atlas Construction).",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "deploy": "gh-pages -d gh-pages",
    "release:major": "npm test && npm version major -m \"Released version %s\" && npm publish --access=public && git push --follow-tags",
    "release:minor": "npm test && npm version minor -m \"Released version %s\" && npm publish --access=public && git push --follow-tags",
    "release:patch": "npm test && npm version patch -m \"Released version %s\" && npm publish --access=public && git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atlasoutdoor/Style-Guide.git"
  },
  "author": "David Amato",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/atlasoutdoor/Style-Guide/issues"
  },
  "homepage": "https://github.com/atlasoutdoor/Style-Guide#readme",
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
